# Wireframes

Документы описывают **фактический UI** reference-клиента [`web-legacy`](../../../../web-legacy/).

Источник правды для экранов — [`../pages.md`](../pages.md). Wireframes детализируют layout и поведение на уровне компонентов legacy-клиента.

При расхождении между `pages.md` и wireframes ориентируйтесь на wireframes + код `web-legacy/src/`.
