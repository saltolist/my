# Аналитика канала (`analytics`)

**Reference:** `web-legacy/src/screens/analytics/`, `web-legacy/src/widgets/analytics-dashboard/`

```
┌─────────────────────────────────────────────────────────────────┐
│  Аналитика канала                                    [← Назад]  │
│ ─────────────────────────────────────────────────────────────── │
│  Динамика прироста                    [24ч|7д|30д|90д|Всё ▾]   │
│  [Просмотры] [Подписчики] [Реакции] ...                         │
│  ╭──────────────────────────────────────────────────────────╮   │
│  │           MultiSeriesTrendChart                          │   │
│  ╰──────────────────────────────────────────────────────────╯   │
│  ┌─────────────────────┐  ┌─────────────────────┐               │
│  │ Прирост по метрикам │  │ Реакции             │               │
│  └─────────────────────┘  └─────────────────────┘               │
│  Тепловая карта активности                                      │
│  Лучшие посты за период                                         │
│   1. Синдром чистого листа...                            [→]    │
└─────────────────────────────────────────────────────────────────┘
```

## Шапка

- Заголовок `Аналитика канала`.
- Справа: `← Назад` → `home`.
- **Период не в шапке** (на desktop) — picker внутри карточки «Динамика прироста».
- На mobile период — `PageHeaderSelect` в шапке.

## Периоды

**5 вариантов:** `24 часа`, `7 дней`, `30 дней`, `90 дней`, `Всё время` (`platformAnalyticsPeriods.ts`).

## Блоки экрана

| Блок | Описание |
|---|---|
| **Динамика прироста** | Summary mini-metrics + `MultiSeriesTrendChart`; selector видимых серий метрик |
| **Прирост по метрикам** | `ChannelMetricBars` |
| **Реакции** | `ChannelReactionsPanel` |
| **Тепловая карта активности** | `AnalyticsHeatmap` |
| **Лучшие посты за период** | Таблица с переходом к посту по `→` |

## Метрики в графике

Просмотры, подписчики, реакции, комментарии, репосты, ER — через selector серий (`channelAnalyticsTrend.ts`).

## Чего нет

- Блок **«По рубрикам»** (bar-chart по рубрикам) **не реализован**.
- Данные — mock/seed.
